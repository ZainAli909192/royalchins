import { NextResponse } from "next/server";
import { requireAdmin } from "@/lib/auth/require-admin";
import { findProduct, ProductStoreError, removeProduct, updateProduct } from "@/lib/products/product-store";
import { productApiSchema } from "@/lib/validations/product";
type Context = { params: Promise<{ id: string }> };
export async function GET(request: Request, { params }: Context) { if (!(await requireAdmin(request))) return NextResponse.json({ message: "Unauthorized." }, { status: 401 }); const product = await findProduct((await params).id); return product ? NextResponse.json(product) : NextResponse.json({ message: "Product not found." }, { status: 404 }); }
export async function PATCH(request: Request, { params }: Context) { if (!(await requireAdmin(request))) return NextResponse.json({ message: "Unauthorized." }, { status: 401 }); const parsed = productApiSchema.safeParse(await request.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ message: "Please correct the product details.", errors: parsed.error.flatten().fieldErrors }, { status: 400 }); try { const product = await updateProduct((await params).id, parsed.data); return product ? NextResponse.json(product) : NextResponse.json({ message: "Product not found." }, { status: 404 }); } catch (error) { return NextResponse.json({ message: error instanceof Error ? error.message : "Unable to update product." }, { status: error instanceof ProductStoreError ? error.status : 500 }); } }
export async function DELETE(request: Request, { params }: Context) { if (!(await requireAdmin(request))) return NextResponse.json({ message: "Unauthorized." }, { status: 401 }); const product = await removeProduct((await params).id); return product ? NextResponse.json({ message: `Product \"${product.name}\" deleted successfully.` }) : NextResponse.json({ message: "Product not found." }, { status: 404 }); }
